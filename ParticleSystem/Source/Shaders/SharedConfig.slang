#pragma once

// Shared with C++.

// #define VULKAN_ENABLE_DEBUG_UTILS

// Define only one.
// #define PARTICLE_SYSTEM_SINGLE_PARTICLE
// #define PARTICLE_SYSTEM_STRESS_TEST
#define PARTICLE_SYSTEM_DEMO

static const int RENDERER_MAX_FRAMES_IN_FLIGHT = 2;
static const int PARTICLES_INIT_WORKGROUP_SIZE = 64;
static const int PARTICLE_EMITTER_WORKGROUP_SIZE = 64;
static const uint MAX_PARTICLES = 1000000;
static const uint MAX_EMITTERS = 128;
static const float PARTICLE_SIM_TIME_STEP = 0.02f;
static const float PARTICLE_SIM_RENDER_SCALE = 0.5f;

enum {
    UNIFORM_BUFFER_BINDING,
    PARTICLE_COUNT_BUFFER_BINDING,
    PARTICLE_COUNT_UNIFORM_BUFFER_BINDING,
    INDIRECT_BUFFER_BINDING,
    VELOCITY_MASS_INV_BUFFER_BINDING,
    POSITION_DIST_TO_FORCE_FIELD_INV_BUFFER_BINDING,
    RADIUS_BUFFER_BINDING,
    TIME_LEFT_BUFFER_BINDING,
    DEAD_BUFFER_BINDING,
    ALIVE_BUFFER_1_BINDING,
    ALIVE_BUFFER_2_BINDING,
    EMITTER_IDX_BUFFER_BINDING,
    EMITTER_BUFFER_BINDING,
    RENDER_IMAGE_SAMPLER_BINDING,
    DESCRIPTOR_BINDING_COUNT,
};
